# Tasks: Guest Checkout

- [x] **Task 1: Create GuestRegisterDto** — Create `apps/server/src/modules/auth/dto/guest-register.dto.ts`. Fields: `email` (`@IsEmail()`), `fullName` (`@IsString()`, `@IsNotEmpty()`, `@MaxLength(100)`). Export the class. Follow the same pattern as `register.dto.ts`.

- [x] **Task 2: Add guestRegister method to AuthService** — In `apps/server/src/modules/auth/auth.service.ts`, add `async guestRegister(dto: GuestRegisterDto)`. Logic: check if email exists (throw `ConflictException` if so), generate random password via `crypto.randomBytes(32).toString('hex')`, hash with bcrypt (salt 10), create User with `role: 'USER'`, generate access + refresh tokens using existing `generateAccessToken` and `generateRefreshToken` methods, return `{ accessToken, refreshToken, user: { id, email, fullName, role } }`. Import `crypto` from Node.js. Add `import { GuestRegisterDto } from './dto/guest-register.dto'`.

- [x] **Task 3: Add guestRegister endpoint to AuthController** — In `apps/server/src/modules/auth/auth.controller.ts`, add `@Public() @Post('guest-register')` endpoint with `@Throttle({ default: { limit: 5, ttl: 60000 } })` for stricter rate limiting. Method: `guestRegister(@Body() dto: GuestRegisterDto)` that calls `this.authService.guestRegister(dto)`. Add Swagger decorators (`@ApiOperation`, `@ApiResponse` for 201 and 409).

- [x] **Task 4: Add guestRegister API function on frontend** — In `apps/web/lib/api/auth.ts`, add `export async function guestRegister(email: string, fullName: string)` that calls `POST ${API_URL}/auth/guest-register` with JSON body `{ email, fullName }`. Returns `{ accessToken, refreshToken, user }`. On error, parse response JSON and throw `Error(data.message)`. Follow the same fetch pattern as existing auth API functions in that file.

- [x] **Task 5: Create GuestCheckoutForm component** — Create `apps/web/components/cart/guest-checkout-form.tsx`. Props: `onAuthenticated: () => void`, `onCancel: () => void`. React Hook Form + Zod validation for `email` (valid email) and `fullName` (min 1 char, max 100). Two inputs with labels, a "Continue as Guest" submit button (primary style), and a text link "Already have an account? Log in" that navigates to `/login?redirect=/cart`. On submit: call `guestRegister(email, fullName)`, then call `useAuth().login()` (or equivalent method to store tokens), then call `onAuthenticated()`. Show error message if email conflict (409) — display "This email already has an account" with a login link. Loading state on submit button. Style consistent with existing cart components.

- [x] **Task 6: Integrate GuestCheckoutForm in CartContent** — In `apps/web/components/cart/cart-content.tsx`: add state `showGuestForm: boolean` (default false). In `handleCheckout`, replace `router.push("/login?redirect=/cart")` with `setShowGuestForm(true)`. Add `handleGuestAuthenticated` callback that sets `showGuestForm(false)` and re-calls `handleCheckout()`. Render `<GuestCheckoutForm>` conditionally when `showGuestForm && !isAuthenticated()` — show it between the cart items and the checkout button area (or as an overlay/section). Pass `onAuthenticated={handleGuestAuthenticated}` and `onCancel={() => setShowGuestForm(false)}`.
