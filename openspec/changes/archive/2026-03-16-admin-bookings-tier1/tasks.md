# Tasks: Admin Bookings Management — Tier 1

- [x] **Task 1: Create API client** — `apps/web/lib/api/admin/bookings.ts`. Create TypeScript interfaces (`AdminBooking`, `BookingStats`, `BookingPagination`, `AdminBookingsResponse`, `BookingQueryParams`) matching backend response shape. Implement `getAdminBookings(params, token)` function that builds URLSearchParams, calls `GET ${API_BASE}/admin/bookings` with Bearer token. Follow same pattern as `lib/api/admin/tours.ts`.

- [x] **Task 2: Create BookingStatistics component** — `apps/web/components/admin/bookings/booking-statistics.tsx`. Props: `stats: BookingStats | null`, `loading: boolean`. Render 2 cards: "Total Revenue" (formatted with `Intl.NumberFormat` as USD) and "Active Bookings" (count). Loading state with 2 animated skeleton cards. Match design styling with icon + label + value in rounded-xl border cards. Green accent for revenue, blue for active bookings.

- [x] **Task 3: Create BookingFilters component** — `apps/web/components/admin/bookings/booking-filters.tsx`. Props: `onSearchChange`, `onStatusChange`, `onDateRangeChange`. Search input with magnifying glass icon, debounced at 300ms using `useDebounce` from `use-debounce`. Status filter buttons: All / Pending / Paid / Cancelled / Refunded. Date range with two date inputs (From / To). Search fires via useEffect on debounced value; status and date changes fire immediately. Layout: search on left, filters on right.

- [x] **Task 4: Create BookingTable component** — `apps/web/components/admin/bookings/booking-table.tsx`. Props: `bookings: AdminBooking[]`, `loading: boolean`. Columns: Booking ID (`#${id}`), Customer (avatar with initials fallback + fullName + email), Tour (`schedule.tour.name`), Date & Time (formatted `schedule.startDate`), Amount (currency formatted `totalPrice`), Status (dual badges — booking status + payment status from `payments[0]?.status`). Badge colors: PAID/SUCCESS → green, PENDING → yellow, CANCELLED/FAILED/REFUNDED → red. Loading skeleton rows, empty state, hover effect on rows.

- [x] **Task 5: Wire up page** — `apps/web/app/admin/bookings/page.tsx` (modify existing). Replace "Coming Soon" stub. State: bookings, stats, pagination, queryParams, loading. Keep existing auth guard. `fetchBookings` with `useCallback([queryParams, token])`. Filter callbacks with `useCallback` that reset page to 1. Compose: Header → BookingStatistics → BookingFilters → BookingTable → Pagination. Inline pagination with "Showing X to Y of Z results", numbered page buttons with active highlight, prev/next arrows with disabled states.
