# Tasks: Admin Dashboard — Lite Scope

- [x] **Task 1: Create DashboardModule backend** — Create `apps/server/src/modules/dashboard/` with `dashboard.module.ts`, `dashboard.controller.ts`, `dashboard.service.ts`, and `dto/dashboard-response.dto.ts`. Controller: `GET /api/admin/dashboard` with `@UseGuards(JwtAuthGuard, RolesGuard)` and `@Roles('ADMIN')`, Swagger decorators. Service: `getDashboard()` using `Promise.all` to query Prisma directly — `payment.aggregate` (status SUCCESS, sum amount), `booking.count()`, `tour.count` (status PUBLISHED, deletedAt null), `user.count` (deletedAt null), `booking.findMany` (last 5, orderBy bookingDate desc, include user select id/fullName/email/avatarUrl, include schedule with tour select id/name). Return shape per design.md API Response. Register in `app.module.ts`.

- [x] **Task 2: Create frontend API client** — `apps/web/lib/api/admin/dashboard.ts`. Define `DashboardStats` interface (totalRevenue, totalBookings, activeTours, totalUsers), `RecentBooking` interface (matching API response booking shape), `DashboardResponse` interface (stats + recentBookings). Implement `getAdminDashboard(token): Promise<DashboardResponse>` calling `GET ${API_BASE}/api/admin/dashboard` with Bearer token. Follow same pattern as `lib/api/admin/bookings.ts`.

- [x] **Task 3: Create DashboardStats component** — `apps/web/components/admin/dashboard/dashboard-stats.tsx`. Props: `stats: DashboardStats | null`, `loading: boolean`. Render 4 KPI cards in responsive grid (1→2→4 cols). Each card: colored icon background + label + formatted value. Revenue uses `Intl.NumberFormat` USD currency, others plain `toLocaleString()`. Icons: `payments` (green), `confirmation_number` (blue), `tour` (purple), `group` (orange). Loading state: 4 skeleton cards with pulse animation. Match design mockup card styling (rounded-xl, border, shadow-sm).

- [x] **Task 4: Create RecentBookings component** — `apps/web/components/admin/dashboard/recent-bookings.tsx`. Props: `bookings: RecentBooking[]`, `loading: boolean`. Card with header "Recent Bookings" + "View All" link to `/admin/bookings`. Table columns: Customer (avatar/initials + name + email), Tour (schedule.tour.name), Date (formatted schedule.startDate), Amount (currency formatted totalPrice), Status (badge with colors: PAID→green, PENDING→yellow, CANCELLED/REFUNDED→red). Loading: 4 skeleton rows. Empty: "No recent bookings" message. Booking ID links to `/admin/bookings/{id}` detail page. Reuse `getInitials` pattern from booking-table.tsx for avatar fallback.

- [x] **Task 5: Wire up dashboard page** — `apps/web/app/admin/page.tsx` (modify existing). Replace entire stub. Add auth guard (same pattern as bookings page: check isAuthenticated + ADMIN role). State: `data: DashboardResponse | null`, `loading`, `lastUpdated: Date | null`. `fetchDashboard` with `useCallback` — calls `getAdminDashboard(token)`, sets data + lastUpdated. Header: "Dashboard Overview" + description + "Last updated: {time}" + refresh button (calls fetchDashboard). Quick Actions section: 3 buttons using `Link` — "Add New Tour" (primary style → /admin/tours), "Create Booking" (outline → /admin/bookings), "Invite User" (outline → /admin/users). Compose: Header → DashboardStats → Quick Actions → RecentBookings. Grid layout for bottom section: RecentBookings takes 2/3 width on lg screens.
